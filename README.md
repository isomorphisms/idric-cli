# Idriç CLI

Command-line tools that belong around Idriç but do not belong in the compiler itself.

This repository is intentionally separate from `isomorphisms/Idric`. Cat Food may track this repository as a normal moving checkout; it must not pin the Idriç compiler or ICU as a submodule merely to make the CLI available.
